{
  "name": "offerflow",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:sqlite": "node -e \"const fs=require('fs');fs.copyFileSync('prisma/schema.sqlite.prisma','prisma/schema.prisma');if(!fs.existsSync('.env'))fs.copyFileSync('.env.example','.env');fs.closeSync(fs.openSync('prisma/dev.db','a'))\" && prisma db push && echo Switched to SQLite (local dev)",
    "db:pg": "node -e \"require('fs').copyFileSync('prisma/schema.pg.prisma','prisma/schema.prisma')\" && prisma db push && prisma generate && echo Switched to PostgreSQL and synchronized schema (Vercel deploy)"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "bcryptjs": "^3.0.3",
    "jose": "^6.2.3",
    "next": "^16.2.6",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tailwindcss/postcss": "^4.3.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "playwright": "^1.60.0",
    "prisma": "^6.5.0",
    "tailwindcss": "^4.3.0"
  }
}
