# OfferFlow 开发规则

## 项目概述
OfferFlow 是一款面向求职者的全流程求职管理工具，Next.js 16 + Tailwind CSS 4 + Prisma (Neon PostgreSQL/SQLite) + 自定义 JWT 认证。

## 重要约定

### 部署
- Vercel Hobby 计划部署，函数超时上限 10s
- 生产数据库：Neon PostgreSQL，开发：SQLite（通过 `npm run db:pg` / `db:sqlite` 切换 schema）
- 出现构建缓存问题时用 `vercel --force --prod` 清除缓存

### 代码风格
- React Server Components 优先，客户端组件使用 `'use client'`
- Tailwind CSS 优先，避免内联样式
- 不添加不必要的注释，代码自文档化
- 不创建不必要的抽象，三个类似行优于一个过早抽象

### 二次开发更新说明
- 功能、交互、界面、部署、配置或文档发生有效变化时，同步维护根目录的 `SECONDARY_DEVELOPMENT.md`
- 更新前必须查看实际代码差异，不能只根据提交标题判断修改内容
- 从用户或部署维护者的角度描述结果，不写函数名、变量名或内部实现过程
- 多个提交反复修改同一处时合并为一条，只记录当前版本的最终有效结果；已经完整撤销的中间改动不写入
- 按 `🐞 修复问题`、`✨ 新增功能`、`🚀 优化改进`、`🎨 界面样式` 四类归档，优先补充或修订已有条目，避免重复
- 不遗漏非界面改动，包括权限、安全限制、数据统计口径、数据库、Docker、自动化流程和使用文档
- 审计范围固定从 `be607dd`（含）开始；更新时将文档开头的截止提交和提交总数同步到当前已提交的 `HEAD`
- 如果主要功能范围发生变化，同时更新 `README.md` 顶部的二次开发摘要，保持其与详细说明一致
